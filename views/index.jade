extends layout

block content
  section(class='settings-panel')
  section(class='module module-chat resizable')
    .title-bar
      h3 Chat
    ul.list-view
      - for(var i = 0;i<20;i++)
        li Lorem Ipsum
    .chat-form
        form#chat_form
          input(autocomplete='off',type='text', id='chat_msg', name='chat_msg', placeholder='...')
  section(class='module module-video resizable')
    .title-bar
      h3 Video Conferencing
    .module-inner
        section(class='video-focus')
        section(class='video-tiles') 
          .title-bar
            h3 Collaborators
  section(class='module module-notes resizable')  
    .title-bar
      h3 Notes
    .module-inner
        p .
          Content here
